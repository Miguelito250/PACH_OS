@model IEnumerable<Pach_OS.Models.DetalleVenta>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CantVendida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Productos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Venta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CantVendida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Productos.IdProductos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Venta.IdVentas)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDetalleVenta">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDetalleVenta">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDetalleVenta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
