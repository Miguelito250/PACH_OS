@model IEnumerable<Pach_OS.Models.ProductosInsumo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CantInsumo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Insumos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Productos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CantInsumo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Insumos.IdInsumos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Productos.IdProductos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
